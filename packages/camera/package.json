{
  "name": "@kyxos/render-camera",
  "version": "0.0.0",
  "private": true,
  "description": "Perspective camera, scene framing, and DOM-independent orbit control.",
  "type": "module",
  "sideEffects": false,
  "files": [
    "dist"
  ],
  "exports": {
    ".": {
      "types": "./dist/index.d.ts",
      "import": "./dist/index.js",
      "default": "./dist/index.js"
    }
  },
  "scripts": {
    "build": "tsc -b --pretty false",
    "clean": "tsc -b --clean",
    "typecheck": "tsc -b --pretty false"
  },
  "dependencies": {
    "@kyxos/render-core": "workspace:*",
    "@kyxos/render-math": "workspace:*",
    "@kyxos/render-scene": "workspace:*"
  }
}
