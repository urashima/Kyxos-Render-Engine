{
  "name": "@kyxos/render-visibility",
  "version": "0.0.0",
  "private": true,
  "description": "Mesh Renderer components, frustum culling, and deterministic render queues.",
  "type": "module",
  "sideEffects": false,
  "files": [
    "dist"
  ],
  "exports": {
    ".": {
      "types": "./dist/index.d.ts",
      "import": "./dist/index.js",
      "default": "./dist/index.js"
    }
  },
  "scripts": {
    "build": "tsc -b --pretty false",
    "clean": "tsc -b --clean",
    "typecheck": "tsc -b --pretty false"
  },
  "dependencies": {
    "@kyxos/render-camera": "workspace:*",
    "@kyxos/render-core": "workspace:*",
    "@kyxos/render-geometry": "workspace:*",
    "@kyxos/render-math": "workspace:*",
    "@kyxos/render-scene": "workspace:*"
  }
}
