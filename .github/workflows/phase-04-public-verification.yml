name: Phase 4 public deployment verification

on:
  push:
    branches:
      - agent/phase-04-public-verification
  pull_request:
    branches:
      - main

concurrency:
  group: phase-04-public-verification-${{ github.ref }}
  cancel-in-progress: true

permissions:
  contents: read

jobs:
  verify-public:
    name: Verify deployed Phase 4 and Latest
    runs-on: ubuntu-24.04
    timeout-minutes: 20
    env:
      CI: 'true'
      EXPECTED_COMMIT_SHA: 43d510a12341cafd6cb1aeb917252d93f222b33f
      PAGES_LATEST_PHASE: '4'
      PAGES_PHASES: 0,1,2,3,4
      PUBLIC_PLAYGROUND_URL: https://urashima.github.io/Kyxos-Render-Engine/

    steps:
      - name: Check out deployed Phase 4 source
        uses: actions/checkout@v7
        with:
          ref: 43d510a12341cafd6cb1aeb917252d93f222b33f
          persist-credentials: false

      - name: Install Node.js
        uses: actions/setup-node@v7
        with:
          node-version: 24.14.0

      - name: Enable pinned pnpm
        run: |
          corepack enable
          pnpm --version

      - name: Install dependencies from lockfile
        run: pnpm install --frozen-lockfile

      - name: Install Playwright Chromium
        run: pnpm exec playwright install --with-deps chromium

      - name: Wait for exact public deployment
        shell: bash
        run: |
          base_url="${PUBLIC_PLAYGROUND_URL%/}/"
          for attempt in $(seq 1 24); do
            latest_html="$(curl --fail --silent --show-error --location "${base_url}latest/" || true)"
            phase_html="$(curl --fail --silent --show-error --location "${base_url}phase-4/" || true)"
            if [[ "${latest_html}" == *"${EXPECTED_COMMIT_SHA}"* ]] && [[ "${phase_html}" == *"${EXPECTED_COMMIT_SHA}"* ]]; then
              echo "Exact Phase 4 merge source is publicly deployed."
              exit 0
            fi
            sleep 5
          done
          echo "Public Phase 4 routes did not expose the expected commit within the fail-closed window." >&2
          exit 1

      - name: Run public Chromium and WebGPU operation gate
        run: pnpm exec playwright test --config playwright.pages.config.ts

      - name: Write public verification evidence
        if: ${{ success() }}
        shell: bash
        run: |
          mkdir -p test-results/phase-04/public
          cat > test-results/phase-04/public/verification.json <<EOF
          {
            "schemaVersion": 1,
            "phase": "04",
            "status": "PASS",
            "expectedCommit": "${EXPECTED_COMMIT_SHA}",
            "latestPhase": 4,
            "phases": [0, 1, 2, 3, 4],
            "publicUrl": "${PUBLIC_PLAYGROUND_URL}",
            "workflowRunId": "${GITHUB_RUN_ID}",
            "workflowJob": "Verify deployed Phase 4 and Latest"
          }
          EOF

      - name: Upload public verification evidence
        if: ${{ !cancelled() }}
        uses: actions/upload-artifact@v7
        with:
          name: phase-04-public-verification-${{ github.run_attempt }}
          path: |
            playwright-report/
            test-results/phase-04/public/
            test-results/pages/
          if-no-files-found: ignore
          retention-days: 7
