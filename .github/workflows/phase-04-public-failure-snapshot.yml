name: Phase 4 public failure snapshot

on:
  pull_request:
    branches:
      - main
    paths:
      - .github/workflows/phase-04-public-failure-snapshot.yml

permissions:
  actions: read
  contents: read

jobs:
  snapshot:
    runs-on: ubuntu-24.04
    timeout-minutes: 5
    steps:
      - name: Capture failed public interaction log
        env:
          GH_TOKEN: ${{ github.token }}
        run: |
          set -euo pipefail
          gh run view 30140285927 --repo "${GITHUB_REPOSITORY}" --log-failed > /tmp/public-failure.log
          tail -n 240 /tmp/public-failure.log

      - name: Upload failed log
        uses: actions/upload-artifact@v7
        with:
          name: phase-04-public-failure-log
          path: /tmp/public-failure.log
